import sys
sys.path.insert(0, r'E:\openclaw\tasks')
from task_board import block_task, complete_task

article = """## OpenClaw 子 Agent 调度：sessions_spawn 三步上手教程

**想象这样一个场景：** 你需要同时让 AI 完成三件事——查行情、写文章、发公众号。如果一件一件来，效率太低；如果你自己拆解指令再分别发送，又容易出错。

OpenClaw 的 **sessions_spawn** 功能，就是来解决这个问题的。它允许你创建一个"子 Agent"，把任务甩给它去跑，自己先休息，等结果回来再继续。整个过程只需要三步。

---

### 什么是 sessions_spawn？

sessions_spawn 是 OpenClaw 的子 Agent 调度机制。简单理解，就是"派小弟干活"：

- **主 Agent（你）：** 发号施令，分配任务
- **子 Agent（sessions_spawn）：** 接收任务，执行工作，返回结果

子 Agent 和主 Agent 共享上下文，可以读取你之前聊过的内容，也可以在自己的 session 里调用各种技能。派它出去后，主 Agent 不需要一直盯着，它会自己跑完。

---

### 三步操作：派活 → 等结果 → 收成果

#### 第一步：调用 sessions_spawn，发射子 Agent

在对话中告诉主 Agent："帮我创建一个子 Agent 去处理 XXX"，主 Agent 就会调用 sessions_spawn，在后台启动一个子 Agent 实例。

这一步不需要你写代码，只需要清晰地描述任务目标和约束条件。比如：

> "帮我创建一个子 Agent，专门负责分析今日大盘，写一份简报"

#### 第二步：子 Agent 执行任务

子 Agent 收到任务后开始工作。它会在自己的 session 里独立运行，调用各种工具和技能，完成你交代的事情。这个过程在后台进行，你的对话窗口可以继续做其他事。

#### 第三步：sessions_yield 交回结果

子 Agent 完成任务后，会通过 sessions_yield 将结果返回给主 Agent。结果会自动出现在你的对话里，就像子 Agent "yield" 回来一样。**注意：yield 后必须等"完成事件"，不能直接往下走**，否则消息会截断。

---

### 踩坑提醒：这些坑别踩

**坑一：yield 后急躁，直接往下走**

很多人在子 Agent 执行完后，急着问"结果呢？"然后继续发消息。正确的做法是：**等待完成事件自动推送**，不要主动轮询或继续发指令。

**坑二：任务描述太模糊**

子 Agent 接收不到你脑子里的上下文。派活时务必写清楚：
- **背景** — 为什么要做这件事
- **目标** — 交付什么
- **约束** — 有什么限制条件

模糊的指令 = 模糊的结果 = 返工。

**坑三：子 Agent 跑一半，人没了**

子 Agent 有独立的 session 生命周期，任务一旦派出，尽量让它跑完再收结果。中途打断可能导致结果不完整。

---

### 适用场景

**适合用 sessions_spawn 的场景：**
- 并行执行多个独立任务（同时查行情 + 写文章）
- 复杂任务拆解，让专业 Agent 处理专业部分
- 需要较长执行时间的任务（不想在聊天窗口干等）

**不适合的场景：**
- 简单一句话就能搞定的事（直接问就行，别派小弟）
- 需要随时干预的实时任务
- 跨 Agent 高度耦合的协作（目前 sessions_spawn 是单向结果返回）

---

**总结：** sessions_spawn 就像给你的 AI 团队配了一个可靠的助理——你做决策，它跑腿。三个步骤：派活、等待、收获结果。上手之后，你会发现多任务并行从来没这么顺过。

"""

# 完成任务
complete_task('T007', 'writer', '文章正文：\\n' + article)
print("文章已提交")
